<div class="row">
  <div class="small-10 column small-centered">

    <h1>Recipes Index</h1>

    <ul>
      <% @recipes.each do |r| %>
      <li><a href="<%= recipe_path(r) %>"><%= r.name %></a></li>
      <% end %>
    </ul>

  </div>
</div>
